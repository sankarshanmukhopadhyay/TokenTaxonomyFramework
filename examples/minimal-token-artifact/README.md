# Minimal Token Artifact Example

A minimal token artifact contribution should include:

- a machine-readable JSON artifact;
- any supporting proto definitions, if required;
- documentation explaining the token purpose and reuse boundary;
- validation evidence from the repository scripts.

This example is intentionally descriptive rather than normative. Use the current artifact conventions in `artifacts/` as the source of truth.
